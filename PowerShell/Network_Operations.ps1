# Проверка доступности хоста по его IP-адресу
Test-Connection -ComputerName 192.168.1.1

# Проверка маршрутизации к хосту
Test-NetConnection -ComputerName example.com

# Копирование файла на удаленный сервер по SSH (например, используя WinSCP)
# Эта операция не является стандартной в PowerShell, но может быть выполнена с помощью сторонних инструментов.
